{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build WASM assembly",
            "type": "shell",
            "command": "wasm-pack build --target web --dev",
            "options": {
                "cwd": "app",
            },
            "problemMatcher": null
        }
    ]
}